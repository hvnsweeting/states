# {{ salt['pillar.get']('message_do_not_modify') }}

start on (net-device-up
          and local-filesystems
          and runlevel [2345]
          and started rsyslog)
stop on runlevel [!2345]

respawn

chdir /usr/local/webdav
setuid webdav
exec /usr/local/webdav/webdav -config /etc/webdav/config.json
